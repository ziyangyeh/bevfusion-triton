# @package _global_
defaults:
  - _self_

dataloader:
  train:
    batch_size: 4
    shuffle: true
  val:
    batch_size: 4
    shuffle: false
  test:
    batch_size: 4
    shuffle: false
  num_workers: 4
  pin_memory: true
